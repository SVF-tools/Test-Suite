# Cases where FullSparse mode (-ae-sparsity=sparse) still fails compared
# to dense/semi-sparse.  One bc basename per line; '#' comments and blank
# lines ignored.  Consumed by Test-Suite/CMakeLists.txt (ae_full_sparse_* targets).

# Andersen field-insensitive: *p resolves to base, AE expands to all
# sibling Geps; JOIN with init values blows the asserted Gep field's
# interval back to TOP at the cmp.
BASIC_array_int_0-0.c.bc

# scanf/fscanf without an extapi.c MEMSET-style annotation widen input
# to TOP, so svf_assert(x == const) after read can't be verified.
INTERVAL_test_19-0.c.bc
INTERVAL_test_2-0.c.bc
CWE126_Buffer_Overread__CWE129_fscanf_01.c.bc

# Detector-mode ptr branch tests: branch narrowing on the loaded ptr
# ValVar doesn't survive to the detector's getAbsValue at the next
# CallICFGNode under FullSparse's ValVar def-site reads.
char_ptr_branch.c.bc
dangleptr_safe_branch.c.bc
int_ptr_branch.c.bc

# Recursion + widen-narrow: post-recursion ICFG node's state doesn't
# pick up the narrowed cycle result, svf_assert reads ⊥ / TOP.
recursive_afterrec_0.c.bc
recursive_afterrec_1.c.bc
recursive_afterrec_3.c.bc
recursive_afterrec_4.c.bc
